server:
  port: 9091

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://config-server:8888
    discovery:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
