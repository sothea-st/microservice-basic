server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sothea-st/microservice-basic.git
          default-label: main
          clone-on-start: true
